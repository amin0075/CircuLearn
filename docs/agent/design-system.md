# Design system

- Reuse primitives in `src/components/ui` and existing global components.
- Treat `src/styles/tokens` as the color, spacing, and theme source of truth.
- Preserve the educational, legible interface and keyboard-accessible circuit controls.
- Respect reduced-motion and keep simulator feedback understandable without color alone.
